# tp_movies_mern
tp movies avec la stack mern (Ecole-IPSSI)


importer les données en faisant un pip install -r requirements.txt, puis changer la variable api key dans le fichier import_movies.py.
npm i puis npm start dans le dossier frontend, puis npm i et node server.js dans le dossier backend 
